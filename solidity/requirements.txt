# hypeback solidity deploy/verify toolchain.
# Install with: pip install -r solidity/requirements.txt
#
# Required for scripts/deploy.py native path (Anvil / Hardhat / any JSON-RPC):
#   - eth-account: transaction signing via Account.sign_transaction
#   - eth-keys:    used by the stdlib-only fallback signer (private-key
#                  → recoverable signature) if eth-account is unavailable.
#   - rlp, eth-utils: RLP encoding + keccak (also transitive via eth-account).
eth-account>=0.14.0
eth-keys>=0.7.0
rlp>=4.0.0
eth-utils>=5.0.0
# Required for tests/mock_provider.py and the legacy web3 deploy path:
web3>=6.0.0
# Required for scripts/compile.py, build.py, deploy.py, verify.py:
py-solc-x>=1.12.0
eth-abi>=5.0.0
packaging>=24.0
